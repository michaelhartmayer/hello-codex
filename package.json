{
  "name": "tools-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "echo \"No tests\""
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "typescript": "^5.2.2"
  }
}
